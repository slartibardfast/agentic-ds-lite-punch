[receipt "plan/0006#reserve-console"]
    disposition = done
    verify = uci show dhcp lists the host entry and the console holds the
    inputs = /etc/config/dhcp
    evidence = dhcp.@host[20] name=ps3 mac=00:24:8D:49:6E:87 ip=192.168.21.138; console answers ping; dnsmasq running; relay untouched
    tool = host-lifecycle@0.53.0
    recorded = 2026-09-13

[receipt "plan/0006#console-via-eth1"]
    disposition = done
    verify = the pbr rule for the console saddr is in nft list ruleset;
    inputs = /etc/config/network, the nft pbr chains and the ip rule tables
    evidence = ip rule 25000 from 192.168.21.138 lookup 1000 (default via 192.168.0.1 dev eth1); route get console src via eth1, untagged lan src unchanged via vdsl4
    tool = host-lifecycle@0.53.0
    recorded = 2026-09-13

[receipt "plan/0006#retarget-relay"]
    disposition = skip
    reason = call/0014
    tool = host-lifecycle@0.53.0
    recorded = 2026-09-13

[receipt "plan/0006#a2-run"]
    disposition = done
    verify = attested operator
    inputs = the two pcaps, the console's reported NAT type
    evidence = organic PSN Type 2 and CoD Open, capture-attributed; exam record
    tool = host-lifecycle@0.53.0
    recorded = 2026-09-13

[receipt "plan/0006#a2-verdict"]
    disposition = done
    verify = attested operator
    inputs = the pcaps
    evidence = call/0014 acceptance reframe
    tool = host-lifecycle@0.53.0
    recorded = 2026-09-13

[receipt "plan/0006#igd-decision"]
    disposition = done
    verify = attested call/NNNN (this milestone's decision in call/)
    inputs = the verdict record
    evidence = call/0015 phase-E priority
    tool = host-lifecycle@0.53.0
    recorded = 2026-09-13

[receipt "plan/0006#igd-decision"]
    disposition = done
    verify = attested call/0015
    inputs = the verdict record
    evidence = call/0015 phase-E priority
    tool = host-lifecycle@0.53.0
    recorded = 2026-09-13

[receipt "plan/0007#migrate-crate"]
    disposition = done
    verify = the worktree software/ds-lite-punch/main holds src/, deploy/
    inputs = the rope-agentic monorepo sources (tools/ds-lite-punch/)
    evidence = crate landed cb7bfb4, recipe live, artifact verified 7127f4bf, software check clean
    tool = host-lifecycle@0.53.0
    recorded = 2026-09-13

[receipt "plan/0007#pin-hunch"]
    disposition = done
    verify = the results record (results/<date>-pinning.md) carries the
    inputs = the router line, the STUN echo mechanics of the A1 assertion
    evidence = RESULTS-2026-09-13-pinning.md: IP session-pinned, keepalive pins the relay port
    tool = host-lifecycle@0.53.0
    recorded = 2026-09-13

[receipt "plan/0007#c3-tcp"]
    disposition = done
    verify = the C3 run record with the binary-searched TCP mapping death
    inputs = plan/0005-test-rig/router-run-c3.py, the rig containers
    evidence = RESULTS-2026-09-13-c3.md: AFTR TCP idle lifetime in (120,300] s, unlock discharged
    tool = host-lifecycle@0.53.0
    recorded = 2026-09-13

[receipt "plan/0007#tcp-datapath"]
    disposition = done
    verify = a granted TCP slot forwards an external TCP connection to the
    inputs = the relay source, the C3 result
    evidence = RESULTS-2026-09-13-tcp-datapath.md: external end-to-end TCP session through the mapping proven (us-wireguard peer), egress fix 6c3d15a, 54 tests, 32/32 Kani
    tool = host-lifecycle@0.53.0
    recorded = 2026-09-13

[receipt "plan/0007#facade"]
    disposition = done
    verify = upnpc -l / -a / -d full verb set with captures; M-POST parity
    inputs = the relay source, the phase E spec, the pinning result
    evidence = RESULTS-2026-09-15-facade-review.md: 5 criticals + approved suggestions fixed (24f4e4d, 34d48c1), 86 tests green; live-rig walk build 92ac62cc; kani deferral call/0019
    tool = host-lifecycle@0.53.0
    recorded = 2026-09-15

[receipt "plan/0007#closure"]
    disposition = done
    verify = host-lifecycle tasks reports every task discharged; validate
    inputs = the milestone records, the decisions
    evidence = RESULTS-2026-09-15-facade-review.md: every task discharged (validate plan/ call/ ok, verify recheck green, book renders); E6 PS3-with-facade run deferred by operator decision (recorded, not attested); kani deferral call/0019
    tool = host-lifecycle@0.53.0
    recorded = 2026-09-15

[receipt "plan/0007#closure"]
    disposition = done
    verify = host-lifecycle tasks reports every task discharged; validate
    inputs = the milestone records, the decisions
    evidence = RESULTS-2026-09-15-facade-review.md + RESULTS-2026-09-15-ps3-facade.md: every task discharged (validate plan/ call/ ok, verify recheck green, book renders); E6 completed 2026-09-15 against deployed 34d48c1 (operator attestation, three-plus games, zero log anomalies in the game window); kani deferral call/0019
    tool = host-lifecycle@0.53.0
    recorded = 2026-09-15

[receipt "plan/0008#disc-presentation"]
    disposition = done
    verify = cargo test (the section 24 matrix, burst rows included);
    inputs = mdbook plan text, the deployed facade code
    evidence = component d5bf749, 101 tests green, clippy clean: version-aware SearchTarget, discovery burst (DISCOVERY_DEBOUNCE=1s, ssdp:all deferred, :2 flips, :1 inert), /igd/v1+v2 URLs, IGD_V2 gate off (R5/R6), classify whitelist extended; section 24 matrix + deadline assertions covered
    tool = host-lifecycle@0.53.0
    recorded = 2026-09-16

[receipt "plan/0008#v2-service-set"]
    disposition = done
    verify = the section 24 v2 rows; the section 26.19 conformance suite;
    inputs = the DeviceProtection:1 service specification PDF, internalized
    evidence = component 4d0160f (host pin 4734203): IGD_V2_ENABLED on; section 24 discovery rows asserted by ssdp_loop_answers_the_mounted_surface (explicit IGD:2 and WIP:2 answered from v2 with the v2 LOCATION; a bare ssdp:all deferred to the debounce deadline and answered v1; an in-window :2 flips both answers to v2) and the document rows by versioned_doc_routing (v2 root desc carries DeviceProtection:1 beside WANIPConnection:2; /igd/v2/WANIPCn.xml publishes the 14 required actions; /igd/v2/DP.xml the 13); dp19_conformance_suite and dp_boundary_wire green; wip2_connection_actions_wire drives the four connection actions (731 ReadOnly, 704 with no tuple, RSIP 0 / NAT 1, 501); cargo test = 116 passed, 0 failed, 1 ignored. OPEN in this receipt: SendSetupMessage dispatches with 600 for an unknown ProtocolType and 704 for WPS, because the WPS Registration Protocol of Appendix A needs the Wifi Alliance specification (not internalized) and a certificate-authenticated TLS channel this plain-HTTP facade does not carry.
    tool = host-lifecycle@0.53.0
    recorded = 2026-09-16

[receipt "plan/0008#canonical-api"]
    disposition = done
    verify = unit tests for allocate_exact versus allocate_preferred
    inputs = the current upsert/add_mapping paths
    evidence = component b22a96d (host pin 266a6d6): the engine is reached through allocate_exact (WIP1/WIP2 AddPortMapping) and allocate_preferred (WIP2 AddAnyPortMapping) over the one mapping table; preferred_port is the pure resolution and allocate_exact_and_preferred_differ asserts the contrast on one shared table state (a port another client holds moves the request to a free port and the occupant survives, while exact surrenders the occupant to the caller for teardown); cargo test = 117 passed, 0 failed, 1 ignored.
    tool = host-lifecycle@0.53.0
    recorded = 2026-09-16

[receipt "plan/0008#v2-service-set"]
    disposition = done
    verify = the section 24 v2 rows; the section 26.19 conformance suite;
    inputs = the DeviceProtection:1 service specification PDF, internalized
    evidence = component 4d0160f (pin updated to 979aafd for the transcription citation; host pin 266a6d6..979aafd): IGD_V2_ENABLED on; section 24 discovery rows asserted by ssdp_loop_answers_the_mounted_surface and the document rows by versioned_doc_routing (v2 root desc carries DeviceProtection:1 beside WANIPConnection:2; /igd/v2/WANIPCn.xml publishes the 14 required actions; /igd/v2/DP.xml the 13); dp19_conformance_suite, dp_boundary_wire and wip2_connection_actions_wire green; cargo test = 117 passed, 0 failed, 1 ignored. The SendSetupMessage WPS limb is no longer an open question: it is decided in call/0021 (advertise WPS as 2.4.3.1 mandates, answer a setup attempt 704 Processing Error with an empty OutMessage, 600 rejected), recorded at plan/0008 section 27.3e and in the component transcription docs/upnp-dp1/TRANSCRIPTION.md.
    tool = host-lifecycle@0.53.0
    recorded = 2026-09-16

[receipt "plan/0008#disc-presentation"]
    disposition = done
    verify = cargo test (the test matrix, burst rows included);
    inputs = mdbook plan text, the deployed facade code
    evidence = component d5bf749, 101 tests green, clippy clean: version-aware SearchTarget, the discovery burst (DISCOVERY_DEBOUNCE=1s, ssdp:all deferred, the :2 flip, :1 inert), the versioned /igd URLs, the IGD_V2 gate off at the time, the classify whitelist extended; the test matrix and the deadline assertions covered. Re-recorded after the naming-audit rename renamed this task's verify text; the claim itself is unchanged.
    tool = host-lifecycle@0.53.0
    recorded = 2026-09-16

[receipt "plan/0008#v2-service-set"]
    disposition = done
    verify = the test matrix's v2 rows; the conformance suite;
    inputs = the DeviceProtection:1 service specification PDF, internalized
    evidence = component 4d0160f (host pin 979aafd): the IGD_V2 gate on; the test matrix' discovery rows asserted by ssdp_loop_answers_the_mounted_surface and the document rows by versioned_doc_routing; dp19_conformance_suite, dp_boundary_wire and wip2_connection_actions_wire green; cargo test = 119 passed, 0 failed, 1 ignored. The WPS limb is decided in call/0021. Re-recorded after the naming-audit rename renamed this task's verify text; the claim itself is unchanged.
    tool = host-lifecycle@0.53.0
    recorded = 2026-09-16

[receipt "plan/0008#bench-matrix"]
    disposition = done
    verify = the Xbox-class, Syncthing-class, Tailscale-class and
    inputs = the deployed facade, the console chain, a v2-capable client
    evidence = Deployed 2026-09-17: artifact built to the recorded recipe from component 2158485 (sha256 c9b246c5..3181db41, static musl, 987872 bytes), installed as /usr/bin/ds-lite-punch (md5 396a22c3..feca8718), previous binary kept at /root/ds-lite-punch.prev; service enabled under procd; the v2 mount live on br-lan 192.168.21.1:49152. Bench: bench-matrix.py (workstation vantage, a LAN caller at 192.168.21.97 as the device sees it) with bench-lan-client.sh (router vantage); 31 of 31 probes pass, recorded in RESULTS-2026-09-17-bench-matrix.md. Highlights: IGD:2 and WIP:2 resolve to the v2 description; a bare ssdp:all is deferred and answered v1 at 1004 ms; an in-window :2 flips both answers to v2; all four v2 mapping mutators 606 unauthenticated; another client's entry read 606, the contained enumeration 714, the listing 730; 731 ReadOnly, 501 ForceTermination, 200 RequestConnection, RSIP 0 / NAT 1; the PKCS5 ceremony reaches Basic over the wire and the lift then answers the same reads 200 on either face; the caller's own mapping is created and deleted without touching the two pre-existing household entries (3074->192.168.21.138, 14572->192.168.21.12, both lease 4294967295). The DP store was seeded out of band for the authenticated rows and removed after the run, so the deployed default is fail-closed; the absent in-band bootstrap is recorded in the results as the shadow of call/0021.
    tool = host-lifecycle@0.53.0
    recorded = 2026-09-17

[receipt "plan/0009#allowlist"]
    disposition = done
    verify = `nft -c` accepts the ruleset; a named device's quiet UDP flow
    inputs = the accepted ruleset shape (the `ct timeout` objects with
    evidence = the policy is installed and read back from the live table (ruleset_in_force true); a named device's quiet flow read 296 s and was alive at t+65 s, the same device unnamed read 55 s and was gone; results/RESULTS-2026-09-18-acceptance.md
    tool = host-lifecycle@0.53.0
    recorded = 2026-09-18

[receipt "plan/0009#snoop"]
    disposition = done
    verify = the observation log carries a learned external tuple for a named
    inputs = the change-data-capture backends in `cdc.rs` (the nft
    evidence = the arm claimed a named flow, learned its tuple (agreeing with the client's own STUN read) and reported the vote decision; a re-key is now reported per observation (observe_report, tested); results record
    tool = host-lifecycle@0.53.0
    recorded = 2026-09-18

[receipt "plan/0009#signal"]
    disposition = done
    verify = a real control point subscribes, then a mapping is added, deleted
    inputs = `#snoop` for the change events, the subscription and notify path
    evidence = a real control point subscribed with a router-side callback; the initial event carried the four declared variables, and add then delete carried only the two that moved; a contained subscriber's count was its own namespace alone
    tool = host-lifecycle@0.53.0
    recorded = 2026-09-18

[receipt "plan/0009#pcp"]
    disposition = done
    verify = a PCP client creates, renews and deletes a mapping against the
    inputs = the listener, quota and result-code notes in plan/0004's
    evidence = the client-visible surface read back from the router with deploy/pcp-probe.py: the drop-then-retry rule, the four refusals, and NAT-PMP on the same socket; results/RESULTS-2026-09-17-implementation.md
    tool = host-lifecycle@0.53.0
    recorded = 2026-09-18

[receipt "plan/0009#tcp-answer"]
    disposition = done
    verify = a decision record states the choice, its cost and its
    inputs = the measured AFTR TCP lifetime (between 120 and 300 seconds, on
    evidence = call/0026 states the decision: a mapping the daemon terminates is held, a client's own connection is signalled and never held, and the surrogate is scoped into its own milestone
    tool = host-lifecycle@0.53.0
    recorded = 2026-09-18

[receipt "plan/0009#collisions"]
    disposition = done
    verify = a fresh grant on a port a live tuple holds takes another port and
    inputs = the change-data-capture mirror the arm already reads, the lease
    evidence = the allocator steers around live tuples and says so (slot 40003 steered around the live tuple(s) [40001]), and the arm reads the live lease table each tick; call/0027 states the rules with their experiments
    tool = host-lifecycle@0.53.0
    recorded = 2026-09-18

[receipt "plan/0009#acceptance"]
    disposition = done
    verify = the external vantage reaches a held mapping at 30, 60 and 120
    inputs = the rig and the external vantage the datapath work already uses
    evidence = the external vantage's probes reached the held mapping at t0+31.4, 61.4, 121.4 and 301.4 s of client silence and were forwarded to the client with the sender preserved; the event surface was driven by a real control point
    tool = host-lifecycle@0.53.0
    recorded = 2026-09-18

[receipt "plan/0009#udp-threshold"]
    disposition = done
    verify = a results record states the death point for a held flow and for
    inputs = the external vantage, the rig, the observation log from `#snoop`
    evidence = an unheld flow's mapping was alive at t0+13.4 s and gone by t0+21.3 s on validated captures, so the AFTR reaps between thirteen and twenty-one seconds; the held flow was still reachable at 301.4 s
    tool = host-lifecycle@0.53.0
    recorded = 2026-09-18

[receipt "plan/0009#collisions"]
    disposition = done
    verify = a fresh grant on a port a live tuple holds takes another port and
    inputs = the change-data-capture mirror the arm already reads, the lease
    evidence = the allocator steers around live tuples and says so (the box line from 2026-09-17); the lease marks a device flow on its bind tuple, moves to a probe-free port keeping client, internal port and label, reports the move, and rolls back when a move cannot complete (182 tests, seven new); the deployed detection ran over live traffic and correctly stayed silent on the slot's own punch
    tool = host-lifecycle@0.53.0
    recorded = 2026-09-18

[receipt "plan/0010#vantage"]
    disposition = done
    verify = the recorded vantage answers on its public address, and a datagram
    inputs = the vantage reference in MEMORY, the policy route and synthetic
    evidence = the vantage egresses as 170.9.238.141, its route to the AFTR address leaves enp0s6, and its first probe to a held mapping arrived on eth1 at 1789905112.87
    tool = host-lifecycle@0.53.0
    recorded = 2026-09-20

[receipt "plan/0010#eif-now"]
    disposition = done
    verify = a held mapping from a synthetic allowlisted client answers a probe
    inputs = the synthetic client, its allowlist entry, its policy route, the
    evidence = four stranger probes from 170.9.238.141:54372 arrived on eth1 (dst 192.168.0.21.40002) at 30/60/120/300 s of the client's silence; plan/0010 results/RESULTS-2026-09-20-stranger-probe.md
    tool = host-lifecycle@0.53.0
    recorded = 2026-09-20

[receipt "plan/0010#watch-design"]
    disposition = done
    verify = attested call/0033, which names the sender, the observer, the
    inputs = the `#eif-now` record, the daemon's observation arm (`obs.rs`
    evidence = call/0033 accepted: a cooperating helper on the vantage sends a marked probe on a fifteen minute interval, the daemon records its arrival as carrier-probe and raises carrier-silent after three misses
    tool = host-lifecycle@0.53.0
    recorded = 2026-09-20

[receipt "plan/0010#watch-design"]
    disposition = done
    verify = attested call/0033
    inputs = the `#eif-now` record, the daemon's observation arm (`obs.rs`
    evidence = call/0033 accepted: a helper on the vantage sends a marked probe on a fifteen minute interval; the daemon logs carrier-probe on arrival and carrier-silent after three misses
    tool = host-lifecycle@0.53.0
    recorded = 2026-09-20

[receipt "plan/0010#watcher"]
    disposition = done
    verify = the component's suite covers the recognition and the absence alarm,
    inputs = the wire shape from `#watch-design`, the component's observation arm
    evidence = pin 0c7085ab, artifact 33091964; the counter, the counting rule and the poll are built with 205 tests; the lane built and published the bytes and the router was installed from them
    tool = host-lifecycle@0.53.0
    recorded = 2026-09-20

[receipt "plan/0010#alarm-proof"]
    disposition = done
    verify = with the helper silent, the daemon reports the loss within the
    inputs = the same rig, the helper held back by hand, the interval fixed by
    evidence = carrier-probe count 1 epoch 1789907858; carrier-silent last_probe 1789907858 waited 60 epoch 1789907918; plan/0010 results/RESULTS-2026-09-20-alarm-proof.md
    tool = host-lifecycle@0.53.0
    recorded = 2026-09-20

[receipt "plan/0010#record"]
    disposition = done
    verify = the results file and the MEMORY entry exist, the design decision of
    inputs = the arrivals, the log excerpts, the vantage and interval used
    evidence = the two results files, this milestone's status line, the record's pin and artifact hash, and the MEMORY entry of 2026-09-20
    tool = host-lifecycle@0.53.0
    recorded = 2026-09-20

[receipt "plan/0010#inbound-translation"]
    disposition = done
    verify = a lease's arrival reaches the LAN at the client's own port, pasted
    inputs = the slot ports and the arm's own pin (`src/nft.rs`), the accept-set
    evidence = the ingress translation is deployed (pin 5214039b, artifact 0abae591, 210 tests); a stranger's datagram to a lease's external tuple appeared on br-lan at the client's own port and its conntrack reply tuple names that port
    tool = host-lifecycle@0.53.0
    recorded = 2026-09-20

[receipt "plan/0010#lease-churn"]
    disposition = done
    verify = a lease granted from a named client keeps its accept element, its
    inputs = the surrender path's bind port (`revoke_datapath`'s call sites in
    evidence = pins 8f179c7 and 0b72060b (artifact f4499c2c, 211 tests): the failing test showed the second no-preference request returning the first lease's slot; fixed, three leases coexist with three translations, a stranger's datagram reaches a listener bound to the lease's own port, and a restart restores the translation with no pin
    tool = host-lifecycle@0.53.0
    recorded = 2026-09-20

[receipt "plan/0010#watch-forward-path"]
    disposition = done
    verify = with the watch armed, a marked datagram from the helper raises the
    inputs = the counting rule's chains (`src/nft.rs`, `CARRIER_CHAINS`), the
    evidence = pins 2b6e0151 and 5df88c78 (artifact a452cf38, 215 tests): the rule goes into the forward path too, the forward rule says meta l4proto udp with no port match, and an install replaces a stale variant; with the watch armed the daemon installed both rules itself, a probe raised the counter to packets 3 bytes 132, and the daemon logged carrier-probe count 3
    tool = host-lifecycle@0.53.0
    recorded = 2026-09-21

[receipt "plan/0011#argdoc"]
    disposition = done
    verify = the generator rewrites its two outputs and leaves the working tree
    inputs = the parser's flag set in `src/main.rs` (`parse_args_from`), the flag
    evidence = crate tools/argdoc lands at 856299c: clap 4.6.7 and clap_mangen 0.3.3 in a crate with its own empty [workspace] table, so the shipping Cargo.toml and Cargo.lock are untouched (git diff --stat empty) and the daemon links nothing from it; the generator writes src/help.txt and deploy/man/ds-lite-punch.8 and rewrites both identically on a second run; the page renders as ds-lite-punch(8) with man --warnings reporting nothing
    tool = host-lifecycle@0.54.2
    recorded = 2026-09-22

[receipt "plan/0011#help"]
    disposition = done
    verify = `ds-lite-punch --help` prints the generated text with a description
    inputs = the generated help text, `usage()` and the error path in
    evidence = 26f213e: ds-lite-punch --help prints the generated 99-line text on stdout and exits 0, --version prints ds-lite-punch 0.1.5 and exits 0, and an unknown flag prints error: unknown argument --nope with the same text on stderr and exits 2; the new test the_help_names_exactly_the_flags_the_parser_takes reads the parser arms from the source above the test module and passes, after being shown failing in both directions (a flag named in the help that the parser does not take, and a flag taken that the help does not name)
    tool = host-lifecycle@0.54.2
    recorded = 2026-09-22

[receipt "plan/0011#man"]
    disposition = done
    verify = `mandoc -T lint` and `groff -man -Tascii` report nothing on
    inputs = the `clap_mangen` output, the init script's flag mapping, the carrier
    evidence = 0f91786: deploy/man/ds-lite-punch.8 renders as ds-lite-punch(8) in System Manager's Manual with groff -man -Tascii clean and man --warnings reporting nothing; the page names every flag the parser takes, and the five sections the generator appends (ENVIRONMENT, FILES, LOG EVENTS, LIMITS, SEE ALSO) are present; clap_mangen's EXTRA block is dropped so the page has one ENVIRONMENT and one SEE ALSO; the test the_man_page_names_the_flags_and_keeps_its_appended_sections reads the roff and was shown failing first when an appended section was renamed
    tool = host-lifecycle@0.54.2
    recorded = 2026-09-22

[receipt "plan/0011#packaging"]
    disposition = done
    verify = `sh -n` accepts the installer, the page lands at
    inputs = `deploy/install.sh`, `.github/workflows/release.yml`, the release
    evidence = f9aff86: sh -n accepts the installer; on the router the page lands at /usr/share/man/man8/ds-lite-punch.8, mode 644, md5 acb9505c13ea88c78cbbfd7321aab78d on both sides; the router carries no man, no mandoc, no nroff and no /usr/share/man at all, so the rendering is the workstation's groff and man --warnings (clean) and the lane's, and the on-box reader is --help printed from the same generated text; release.yml refuses an empty page and uploads it beside the binary and artifact-record.txt; the tagged release that carries it is plan/0011#record's
    tool = host-lifecycle@0.54.2
    recorded = 2026-09-22

[receipt "plan/0011#operator-docs"]
    disposition = done
    verify = `docs/operators/` carries the five pages, every command and path in
    inputs = the generated help and manual page, `deploy/ds-lite-punch.init`
    evidence = 1510879: docs/operators/{install,configure,operate,upgrade,troubleshoot}.md written in the README's STE register for a stranger on another line; every command and path in them was run on the box (netstat -lnup, ss -lnup, nft list table ip dslp, nft list ruleset | grep, ip rule show showing 25100 over table 1001, ip route show table 1001, cat /run/ds-lite-punch/tuple, logread | grep ds-lite-punch, and the man page placed at /usr/share/man/man8 mode 644 md5 acb9505c); host-lint --docs over the five reports clean after three advisory notes (two tricolons and an anaphora list) were reworded; the component README carries one note about staccato paragraphs, which is the terse register it was written for, and the two spec-transcription trees carry the audit's findings as archived third-party text, which plan/0011#lanes excludes; the pages describe the build this milestone produces, and the router still runs 0.1.5, where --version is an unknown argument and --help prints the old one-line usage
    tool = host-lifecycle@0.54.2
    recorded = 2026-09-22

[receipt "plan/0011#site"]
    disposition = done
    verify = the component's Pages address answers 200, the landing page links each
    inputs = `docs/index.md`, the Pages setting on the component repository, the
    evidence = 0c179d3 plus the repository setting: Pages is enabled on the component serving the default branch at /docs, build_type legacy, https enforced; the build status is built and https://slartibardfast.github.io/ds-lite-punch/ answers 200; the landing page links all five operator pages, each of which answers 200 at /ds-lite-punch/operators/<name>.html, and its manual link points at the repository file for deploy/man/ds-lite-punch.8; the repository homepage field names the site. The site also makes the URL the manual page's SEE ALSO cites resolve.
    tool = host-lifecycle@0.54.2
    recorded = 2026-09-22

[receipt "plan/0011#metadata"]
    disposition = done
    verify = `gh repo view` reports the Unlicense, the topics, and the site as the
    inputs = the host's `LICENSE` text, the repository settings
    evidence = 6232dcf plus the repository settings: gh repo view reports licenseInfo unlicense (The Unlicense), homepageUrl https://slartibardfast.github.io/ds-lite-punch/, and eight topics (cgnat, ds-lite, igd, nat-pmp, openwrt, pcp, rust, upnp); LICENSE is the host's own Unlicense text verbatim; the README gained a Documentation section naming the site and the manual page, and a License section pointing at the file
    tool = host-lifecycle@0.54.2
    recorded = 2026-09-22

[receipt "plan/0011#man"]
    disposition = done
    verify = `groff -man -ww -Tascii` reports nothing on
    inputs = the `clap_mangen` output, the init script's flag mapping, the carrier
    evidence = re-recorded after the plan's verify text was corrected to name groff, which is the renderer this project has and the one whose warning path was exercised in both directions
    tool = host-lifecycle@0.54.2
    recorded = 2026-09-22

[receipt "plan/0011#lanes"]
    disposition = done
    verify = the component's lane fails on a deliberate break in the help text, in
    inputs = `.github/workflows/ci.yml`, the man render check, the link check, the
    evidence = 27e6657: the component's ci.yml gained a docs job with four checks, each shown failing before it landed. The generated surfaces: the job regenerates src/help.txt and deploy/man/ds-lite-punch.8 with the host target named against the crate tree's musl pin, then git diff --exit-code, proven by staging a drifted help text (exit 1 with the diff printed). The render: groff -man -ww writes to stderr and exits 0, so the gate is a non-empty stderr file, proven with an undefined macro (troff: warning: macro 'ZZ' not defined). The links: tools/link-check.sh over README.md, docs/index.md and the five operator pages, proven with a renamed target (exit 1, 'broken link: configure-missing.md'). The register: host-lint --docs . pinned by a clone and build of v0.22.0, which resolves to 0eeabc20, the commit the host's own submodule pins, verified locally as the job runs it and reporting two deliberate staccato notes (README.md and docs/index.md) with exit 0, proven by appending a negative-parallelism sentence (exit 3). .host-lintignore names the two archived transcription trees with the reason. The naming half is deliberately not wired: it reports 86 findings over the component, every one a source comment holding an RFC section reference, a numbered UPnP clause, or a pre-adoption plan label, which plan/0011 records as separate work.
    tool = host-lifecycle@0.54.2
    recorded = 2026-09-22

[receipt "plan/0011#record"]
    disposition = done
    verify = the host's pin names the commit that carries the surfaces, the host's
    inputs = the component's lane output, the artifact record, the tagged release
    evidence = the host's pin is f0c65e39, the tagged 0.2.0 commit, with artifact 924281b2f6b544d6da6b17dddd312978560cdb467df58a96f15e9016bbe72db9; the host's reproducible-build lane rebuilt from that pin and reproduced it (run 35713210604, success), and this development host's own software --verify-build reports UNVERIFIABLE for want of a container runtime; the release phase was dispatched before the bump and computed 0.1.5 -> 0.2.0, which is the bump that shipped, and the release receipt records v0.2.0@924281b2... authorized by plan/0011; all nine tasks carry receipts; the results note is plan/0011-operator-surfaces/results/RESULTS-2026-09-22-operator-surfaces.md; release v0.2.0 is published and immutable and carries the binary, ds-lite-punch.8 and artifact-record.txt, all three agreeing on 924281b2... and fetchable without a token
    tool = host-lifecycle@0.54.2
    recorded = 2026-09-22

[receipt "plan/0011#packaging"]
    disposition = done
    verify = `sh -n` accepts the installer, the page is placed at
    inputs = `deploy/install.sh`, `.github/workflows/release.yml`, the release
    evidence = re-recorded after the plan's verify text took plain verbs (the page is placed at, rather than a phrase with land)
    tool = host-lifecycle@0.54.2
    recorded = 2026-09-22

[receipt "plan/0011#site"]
    disposition = done
    verify = the component's Pages address answers 200, the home page links each
    inputs = `docs/index.md`, the Pages setting on the component repository, the
    evidence = re-recorded after the plan's verify text says home page, and the site's own front page is docs/index.md served from docs
    tool = host-lifecycle@0.54.2
    recorded = 2026-09-22

[receipt "plan/0011#record"]
    disposition = done
    verify = the host's pin names the commit that carries the release, the host's
    inputs = the component's lane output, the artifact record, the tagged release
    evidence = re-recorded after the audit took plain words: the milestone is plan/0011-documentation and the record is results/RESULTS-2026-09-22-documentation.md, both renamed with their references; the verify text now says the pin names the commit that carries the release, which is what it does for v0.3.0 at d5657da
    tool = host-lifecycle@0.54.2
    recorded = 2026-09-23
