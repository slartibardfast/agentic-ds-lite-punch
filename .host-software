# .host-software — the Where room recipe (STRUCTURE.md). One stanza per
# component: source URL, the pinned canonical SHA, the canonical branch, any
# parallel-line worktrees, and the reproducibility record.
#
# The ds-lite-punch crate has not yet been migrated into this component repo
# (it lived at `tools/ds-lite-punch/` in the former rope-agentic monorepo);
# until it lands with a build recipe and artifact hash, reproducibility is
# waived by call/0012.
[software "ds-lite-punch"]
	url   = https://github.com/slartibardfast/ds-lite-punch.git
	pin   = f7e11e934e990bcaf43b0ed38d56bc21a9f71e6d
	branch = main
	repro-waiver = call/0012