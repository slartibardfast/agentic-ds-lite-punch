# .host-software — the Where room recipe (STRUCTURE.md). One stanza per
# component: source URL, the pinned canonical SHA, the canonical branch, any
# parallel-line worktrees, and the reproducibility record.
#
# The crate migrated 2026-09-13 (component cb7bfb4) from the former
# rope-agentic monorepo (tools/ds-lite-punch/), unmodified; the musl
# release build reproduces the deployed artifact byte-identically, so the
# waiver (call/0012) is retired by call/0016.
[software "ds-lite-punch"]
	url   = https://github.com/slartibardfast/ds-lite-punch.git
	pin   = acd1162e67b14add21f85b33363759d92c318b1f
	branch = main
	toolchain = x86_64-unknown-linux-musl
	build = cargo build --release --target x86_64-unknown-linux-musl
	artifact = target/x86_64-unknown-linux-musl/release/ds-lite-punch 496d0e291f11cbb9f380667347ad32dea69e296fd6dad4ac7603d499100b63b5
# The artifact hash above was derived on the canonical build host before
# the v2 listing fix (ad69a49) and before plan/0009's hold and shared-port
# work (acd1162), so two code changes have landed since. This development
# host's builds differ from canonical (the deployed acd1162 binary is md5
# cd8e49be, a local build), so a hash recorded from here would be a false
# anchor; the re-derivation is owed on the canonical host, or by
# `software --verify-build` run where it builds.